import { apiBaseUrl, uploadBaseUrl, buildMediaUrl } from '../config/backendConfig';

const Api_url = {
  BACKEND_URI: apiBaseUrl,
  UPLOAD_BASE_URL: uploadBaseUrl,
  buildMediaUrl,
};

export default Api_url;
